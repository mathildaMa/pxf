---
platform: linux

image_resource:
  type: docker-image

inputs:
- name: pxf-protocol-extension_src
- name: gpdb_package

outputs:
- name: dist

params:
  TARGET_OS:
  TARGET_AR:

run:
  path: pxf-protocol-extension_src/concourse/scripts/build.bash
